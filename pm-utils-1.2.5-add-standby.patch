diff -up pm-utils-1.2.5/pm/pm-functions.in.orig pm-utils-1.2.5/pm/pm-functions.in
--- pm-utils-1.2.5/pm/pm-functions.in.orig	2009-03-26 03:14:06.000000000 +0100
+++ pm-utils-1.2.5/pm/pm-functions.in	2014-05-05 13:33:52.522506228 +0200
@@ -260,6 +260,9 @@ if [ -z "$SUSPEND_MODULE" ]; then
 	elif [ -c /dev/pmu ] && pm-pmu --check; then
 		SUSPEND_MODULE="kernel"
 		do_suspend() { pm-pmu --suspend; }
+	elif grep -q standby /sys/power/state; then
+		SUSPEND_MODULE="kernel"
+		do_suspend() { echo -n "standby" >/sys/power/state; }
 	fi
 fi
 
